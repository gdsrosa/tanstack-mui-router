import { createTheme } from "@mui/material"

const theme = createTheme({
  palette: {
    primary: {
      main: "#0D0C1D",
      light: "#26254D",
    },
    secondary: {
      main: "#F1DAC4",
    },
  },
})

export default theme
